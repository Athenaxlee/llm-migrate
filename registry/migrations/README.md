# Migration registry

This directory contains versioned, provenance-backed migration knowledge. Files
are loaded with the model registry and enrich comparisons for the matching
source and target model pair.
